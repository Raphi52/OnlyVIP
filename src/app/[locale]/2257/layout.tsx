import { Metadata } from "next";

export const metadata: Metadata = {
  title: "18 U.S.C. 2257 Compliance | VipOnly",
  description: "Record-Keeping Requirements Compliance Statement for VipOnly",
};

export default function Compliance2257Layout({ children }: { children: React.ReactNode }) {
  return children;
}
